# הקשר הפרויקט — הזמנה אישית (INVITE)

## מה זה — החזון (רן, 2026-07-31)
**השער הקבוע לכל ערב זוגי.** לא חד-פעמי: כל ערב עתידי (סיפור חדש, מסע חדש, RRXO כלשהו) מתחבר דרך ההזמנה. הזרימה הקבועה: הזמנה → היא בוחרת ערב מימים שרן הציע → רמז מסתורי-מפתה מתגבר → ביום עצמו טקס הקדמה (יין, כוסית, הכנה) → הקישור למסע נפתח. לערב חדש מחליפים רק את `STORY` בקונפיג (קישור + רמזים + מועמדים).

## טכנולוגיה
- קובץ יחיד `index.html` — vanilla HTML/CSS/JS, בלי build, בלי תלויות (רק גופן Heebo מגוגל)
- שפת עיצוב משוכפלת מ-**RRXO**: רקע `#050510`, זכוכית blur 24px, גרדיאנט fuchsia↔purple, film grain, word-appear
- Deploy: **GitHub Pages** — ריפו `Raniv123/INVITE`, ענף `main`, root
- **קישור חי:** `https://raniv123.github.io/INVITE/`

## הזרימה
01. מעטפה עם חותם-לב → לחיצה נפתחת
02. לוח שנה: "הוא שמר לך שלושה ערבים. בחרי את שלך" — מועמדים זוהרים
03. בחירה → אישור → חגיגה + כפתור וואטסאפ "עדכני אותו" (wa.me)
04. אחרי בחירה: כרטיס רמז מתגבר (far/near/dayBefore/today) + עיגול ימים גדול + שעון LTR שעות:דקות:שניות
05. ביום עצמו (לפי `Asia/Jerusalem`): חשיפה מלאה + כפתור "פתחי את המסע" → RRXO2
06. עבר התאריך → מצב past עדין

## קונפיג (בראש index.html)
- `STORY` — קישור RRXO2 + רמזי **וילה על המים**
- `CANDIDATE_DATES` — `['2026-08-03','2026-08-04','2026-08-05']` (שני/שלישי/רביעי, בחירת רן 2026-07-31)
- בחירה שלה נשמרת ב-`localStorage['invite-chosen-date']`

## כלים לבדיקה (לרן בלבד)
- `?preview=far|near|dayBefore|today` — כפיית שלב תצוגה
- `?reset=1` — איפוס הבחירה

## הנחיות רן (2026-07-31) — לביצוע כשהיא בוחרת
01. ⏰ **ברגע שרן מדווח על היום שנבחר** → ליצור `2` תזכורות `/alarms` (kind=task): (א) `10:00` בבוקר של היום — "הכנה לערב — לשבת עם קלוד להכין את שלב היום עצמו"; (ב) לפנות ערב — "שלח לה 2626 + פתח RRXO2/#partner"
02. **שלב "היום עצמו" (טקסטים/תמונות/טקס) יעוצב סופית יחד עם רן באותו בוקר** — לא לבנות מראש מעבר לקיים
03. תמונות רמז — מוקפא בינתיים; טקסטים משודרגים (שזורים בסיפור) נוסחו בצ'אט וממתינים לאישור מיקום

## מה נשאר
01. רן בודק את הקישור החי → שולח לה הודעת פתיחה רומנטית (נוסחים הוצעו בצ'אט, טרם נבחר)
02. כשהיא בוחרת ערב → רן מדווח → קובעים צלצול תזכורת (`/alarms`, kind=task) ליום שנבחר: "שלח לה 2626 + פתח #partner"
03. ביום: רן שולח לה `2626`, פותח `raniv123.github.io/RRXO2/#partner` (קוד `6262`)

## ⛔ נקודת עצירה 2026-07-31 לילה — צעד ראשון בפעם הבאה
01. **commit מקומי `0f5b653` (13 תיקוני Codex) לא נדחף** — האתר החי מריץ את הגרסה הקודמת. לפני push: אימות E2E (סקריפט מוכן: scratchpad/verify-invite.mjs — שרת מקומי 3377 + שני דפים: בחירה→פרטנר רואה→ACK→רמז נפתח בלי רענון, בדיקת Escape, preview נקי)
02. אחרי אימות ✓ → push (= דיפלוי) → וואטסאפ לרן שהכול מאומת
03. **ממתין להחלטות רן על תוכנית-חזון.html** (נכתבה, לא אושרה): שיטת הדז'ה-וו — רמזים מתמונות villa-00..10 האמיתיות בזוויות לא מזוהות (רן הגדיר: רמז 1 = הווילה מרחוק, רמז 2 = villa-02 בזווית אחרת). תמונות האווירה הנוכחיות = זמניות
04. הביקורת המלאה של Codex שמורה בהיסטוריית הצ'אט; ממצא 5 (state לפי storyId לערבים עתידיים) ו-8 (ספירה לפי tz מכשיר) — נדחו במודע לעתיד

## מצב 2026-07-31 ערב — מוכן לשליחה
- **פתיחה:** תמונת ים לילי עם אור זהוב (`hint-images/hint-far.jpg`) — נגיעה באור → אדוות → לוח (הוחלפו מעטפה וקופסה; קופסה = דז'ה-וו עם RRXO2)
- **4 תמונות אווירה** (Nano-Banana Pro) בכרטיסי הרמז — מטושטשות עד ACK שלו, מתחדדות בחשיפה. `hint-images/`
- **רמזים סופיים** — מגרים בלי להסגיר את הסיפור (אין פרטי סצנה!). כלל: רמז = תחושה, לא תיאור
- **טקס הקדמה ביום עצמו:** יין → כוסית → הכנה → "והיכנסי" + כפתור המסע
- **ICS** "הוסיפי ליומן" בשני הצדדים; לבבות אדומים; בחירה חד-פעמית נעולה
- סנכרון ntfy `invite-nx7k4qv92m-2026` — CHOSE/ACK, poll 8s, אומת הלוך-חזור
- הודעת פתיחה נוסחה ונשלחה לרן בוואטסאפ; ממתין: הוא שולח לה
- ⛔ **כלל: שום פתיחה על מסך המחשב בלי אישור רן — מסירה בוואטסאפ דרך רן בוט בלבד**

## היסטוריה
- 2026-07-30: נבנה ע"י המעצב, ביקורת Codex (GPT-5.6 Sol) — תוקנו: past-לנצח, עדכון חצות, אזור זמן ישראל, ספירה מחדש (עיגול ימים + שעון LTR לבקשת רן)
- 2026-07-31: זרימת "היא בוחרת" מתוך 3 מועמדים + דיפלוי חי
- סיכון מקובל (כמו RRXO): הקישור והרמזים גלויים ב-source; preview חושף שלבים

📅 עדכון אחרון: 2026-07-31
